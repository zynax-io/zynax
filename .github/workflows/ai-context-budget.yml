name: AI Context Budget

on:
  push:
    branches: [main]
    paths:
      - 'CLAUDE.md'
      - '**/AGENTS.md'
      - 'docs/ai-assistant-setup.md'
  pull_request:
    paths:
      - 'CLAUDE.md'
      - '**/AGENTS.md'
      - 'docs/ai-assistant-setup.md'
  workflow_dispatch:

permissions:
  contents: read

defaults:
  run:
    shell: bash

jobs:
  count:
    name: Count AI context lines
    runs-on: ubuntu-24.04
    container:
      image: ghcr.io/zynax-io/zynax/ci-runner@sha256:cf4972f24c76dc8061dfc3f7aef1b08a1b44ec2947953dbac91bdf51ca62491e
      options: --user root
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Count AI context lines
        id: count
        run: |
          OUTPUT=$(zynax-ci check ai-context)
          echo "$OUTPUT"
          {
            echo "## AI Context Budget"
            echo ""
            echo "$OUTPUT"
          } >> "$GITHUB_STEP_SUMMARY"
        # Advisory only — never fail CI
        continue-on-error: false
