# SPDX-License-Identifier: Apache-2.0
apiVersion: v2
name: zynax-workflow-compiler
description: >
  Helm chart for the Zynax Workflow Compiler — stateless gRPC service that
  parses and validates YAML workflow manifests and compiles them to WorkflowIR.
type: application
version: 0.1.0
appVersion: "0.4.0"
dependencies:
  - name: zynax-lib
    version: "0.1.0"
    repository: "file://../zynax-lib"
