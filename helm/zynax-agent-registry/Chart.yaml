# SPDX-License-Identifier: Apache-2.0
apiVersion: v2
name: zynax-agent-registry
description: >
  Helm chart for the Zynax Agent Registry — stateless gRPC service that
  registers and discovers adapters. Manages agent registrations and provides
  lookup for task-broker capability dispatch.
type: application
version: 0.3.0
appVersion: "0.4.0"
dependencies:
  - name: zynax-lib
    version: "0.1.0"
    repository: "file://../zynax-lib"
